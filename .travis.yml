language: rust

script:
  - cargo build --release --verbose
  - cargo test --verbose
  - tar -cvf algo-linux.tgz -C target/release algo algodata

deploy:
  provider: releases
  api-key: $GITHUB_API_TOKEN
  file: "algo-linux.tgz"
  skip_cleanup: true
  on:
    tags: true
    all_branches: true
